<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.moa.faq.mapper.FaqMapper">
	<select id="selectFaq" resultType="FaqVO">
		SELECT  board_no,
				board_theme,
				mem_code,
				ttl,
				cntn
		  FROM	cus_center	
	</select>
</mapper>